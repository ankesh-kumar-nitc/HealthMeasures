[pytest]
# Runs tests with 2 parallel workers. loadscope keeps each test class/module
# on a single worker since the suite shares one backend instance and assumes
# sequential state within a class. For serial runs use `-n 0`.
required_plugins = pytest-xdist
addopts = -n 2 --dist loadscope
